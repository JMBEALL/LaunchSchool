//PEDAC PRACTICE - lets start at the bery beginning

//input: string
//output: array with palindromes as elements
//rules:
//  explicit requurements:
      // -every palindrome must be a single element (new array);
      // - if there is not one or an empty string is passed in, return an empty array.;
      // -they are case sensitive
      // -