// Write a program that prompts the user for two positive integers, and then prints the results of the following operations on those two numbers: addition, subtraction, product, quotient, remainder, and power. Do not worry about validating the input.

const readline = require("readline-sync");

const num1 = +readline.question("Wnter the first number:\n");

const num2 = +readline.question("Enter the second number:\n");

console.log(num1)








// function log(num1, num2) {
// prompt();
// prompt();
// prompt();
// prompt();
// prompt();
// prompt();
// }

// function prompt(num1,num2,operand) {
// console.log(`=> ${num1} ${operand} ${num2} = ${num1 operand num2}`)
// }